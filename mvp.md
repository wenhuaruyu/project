# Guihua Home MVP 方案

## 1. MVP 目标与边界

### 1.1 目标
- 在最短周期内交付可上线的小程序最小可用版本，优先保障入住前服务与离店前购买转化。
- 保证核心链路可用、支付口径正确、敏感数据安全合规。

### 1.2 本期纳入（MVP）
- 流程 A（入住前）：直接进入 -> 入住信息查看 -> 客户登记提交。
- 流程 C（离店前）：商品浏览 -> 下单 -> 支付 -> 订单查询。

### 1.3 本期不纳入（延期到 V1.1）
- 流程 B（旅游攻略）：攻略列表、筛选、详情、导航。
- 后台可视化 UI 页面（仅保留云函数能力）。
- 非核心体验增强（推荐系统、复杂营销玩法、重视觉动效）。

## 2. MVP 必守口径

- 统一接口响应结构：`code`、`message`、`data`、`requestId`。
- 支付成功最终判定：仅以 `payment-callback` 为准，前端支付结果仅作提示。
- 敏感字段（手机号、证件号、Wi-Fi 密码）必须加密或脱敏。
- 命名边界：API `camelCase`，DB `snake_case`，在云函数边界映射。
- 管理能力只要求云函数端权限与数据写入能力，不要求后台 UI 交付。

## 3. 功能清单（MVP）

### 3.1 流程 A：入住前
- `auth-verify-booking`：保留为可选能力，MVP 默认不作为登录前置条件。
- `store-info-get`：返回门店信息、联系方式、脱敏 Wi-Fi 信息、入住须知。
- `guest-profile-submit`：客户登记提交，敏感信息加密写入。

### 3.2 流程 C：离店前
- `product-query` / `product-detail-get`：商品列表与详情。
- `order-create`：服务端价格与库存校验、订单生成。
- `order-pay`：支付参数生成（不改最终支付状态）。
- `payment-callback`：支付回调确认并落最终状态。
- `order-list-query` / `order-detail-get`：订单列表与详情查询。

### 3.3 支撑能力
- `inventory-lock`：库存锁定与回滚。
- `admin-content-manage`：云函数侧内容管理与权限校验。

## 4. 实施计划（建议 4 天）

### Day 1：工程壳与流程 A 打通
- 补齐小程序工程文件（`app.js`、`app.json`、`app.wxss`、页面配置与基础视图）。
- 联通身份校验、入住信息、登记提交。

### Day 2：流程 C（商品与下单）
- 商品列表/详情展示。
- 下单链路与库存校验联调。

### Day 3：流程 C（支付与订单）
- 支付参数发起联调。
- 回调确认后状态流转、订单查询联调。

### Day 4：收口与提审准备
- 异常场景回归（参数错误/权限错误/库存不足/未回调）。
- 安全复核（敏感字段、日志脱敏、权限）。
- 整理提审材料与 `memory-bank/progress.md` 记录。

## 5. 验收标准（通过即可提审）

### 5.1 主流程验收
- 流程 A 全链路通过：校验成功 -> 查看入住信息 -> 登记成功。
- 流程 C 全链路通过：浏览商品 -> 下单成功 -> 支付回调后订单变为已支付 -> 订单页可查询。

### 5.2 关键口径验收
- 参数错误返回 `40001`。
- 无权限返回 `40301`。
- 库存不足返回 `40901`。
- 前端支付成功提示不改终态；仅回调后改为 `PAID`。

### 5.3 安全与发布验收
- 数据库抽检敏感字段无明文（手机号/证件号/Wi-Fi 密码）。
- 权限校验通过（普通用户不可执行管理操作）。
- `memory-bank/progress.md` 完整记录本期完成内容与验证证据。

## 6. 发布后立即进入的 V1.1

- 补齐流程 B（攻略模块）并打通导航动作。
- 增加后台可视化管理页面（可选）。
- 增补自动化测试与更完整的监控告警策略。
